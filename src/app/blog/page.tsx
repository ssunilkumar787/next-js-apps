import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: {
    absolute: "blog",
  },
};

const Blog = () => {
  return <div>Blog</div>;
};

export default Blog;
